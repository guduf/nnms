{
  "name": "nnms-manager",
  "version": "$NNMS_VERSION",
  "description": "library to manage N&M's process",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/guduf/nnms.git"
  },
  "scripts": {
    "compile": "tsc -p ./tsconfig.json",
    "nnms": "nnms"
  },
  "devDependencies": {
    "nnms-cli": "$NNMS_VERSION",
    "typescript": "^3.5.0"
  },
  "dependencies": {
    "nnms": "$NNMS_VERSION",
    "nnms-common": "$NNMS_VERSION",
    "nnms-process": "$NNMS_VERSION"
  }
}
